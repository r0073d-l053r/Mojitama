<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mojitama.app"
    android:versionCode="25"
    android:versionName="1.8">

    <uses-sdk android:minSdkVersion="24" android:targetSdkVersion="35" />

    <!-- haptic feedback for the in-game vibration setting -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- WebView requires this even though every asset is bundled locally -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- pet-care reminders. Ignored below API 33, where it is implicitly granted. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- alarms do not survive a reboot; re-arm the schedule afterwards -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Deliberately NOT declared: SCHEDULE_EXACT_ALARM (denied by default on
         Android 14+) and USE_EXACT_ALARM (reserved for alarm clocks).
         setAndAllowWhileIdle needs neither and still fires during Doze. -->

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true"
        android:allowBackup="true"
        android:supportsRtl="true">

        <activity
            android:name="com.mojitama.app.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|smallestScreenSize|density"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- fires pet-care reminders, and re-arms them after a reboot or app update -->
        <receiver
            android:name="com.mojitama.app.AlertReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.mojitama.app.ALERT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- home-screen widget. exported=false is correct: widget broadcasts arrive
             from the system uid, and our own PendingIntents run as us. -->
        <receiver
            android:name="com.mojitama.app.MojitamaWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.mojitama.app.WIDGET_FEED" />
                <action android:name="com.mojitama.app.WIDGET_CLEAN" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>
    </application>
</manifest>
